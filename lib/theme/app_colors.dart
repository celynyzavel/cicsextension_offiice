import 'package:flutter/material.dart';

// ============================================================
// APP COLORS — every color constant used across the app lives
// here. If you need to re-theme the app, this is the one file
// to touch.
// ============================================================

const kBackground = Color(0xFF0F1F48);
const kSidebar = Color(0xFF142554);

const kCard = Color(0xFF1C2E63);
const kCardBorder = Color(0xFF2C447B);

const kPrimary = Color(0xFF2954D1);
const kPrimaryHover = Color(0xFF3564E8);

const kInfo = Color(0xFF153B6A);

const kWhite = Colors.white;
const kTextSecondary = Color(0xFFA8B2C7);
const kMuted = Color(0xFF7886A8);

const kGold = Color(0xFFFFC857);
const kOrange = Color(0xFFFF8A3D);
